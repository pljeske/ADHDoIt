services:
  db:
    ports:
      - "5432:5432"
  app:
    build:
      target: builder
    ports:
      - "8080:8080"
    volumes:
      - ./backend:/app
    command: ["air"]
    environment:
      ENVIRONMENT: development
#  frontend:
#    ports:
#      - "5173:5173"
#    command: ["npm", "run", "dev", "--", "--host"]
